const devConfig = {
  oss_setting:{
    // 这里没写是因为github写了推不上
    // 配置环境变量ID。
    // 配置环境变量SECRET。
    // 将<YOUR-BUCKET>替换为Bucket名称。
    bucket: 'liuxu-david'
  }
}

export default () => devConfig